workflows:
  vue-android:
    name: Vue Android
    max_build_duration: 30
    environment:
      node: latest
      vars:
<<<<<<< HEAD
        PACKAGE_NAME: "com.oxygetv.app"
    scripts:
      - name: Install dependencies
        script: |
          npm install
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap init OxygeTV com.oxygetv.app
      
      - name: Build
        script: npm run build
      
      - name: Setup Capacitor
        script: |
          npx cap add android
          npx cap sync
      
=======
        # Определяем переменные здесь
        PACKAGE_NAME: "com.oxygetv.app"
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build
        script: npm run build
>>>>>>> 29ee1b224115e2cbd0fda8c7d2d6682aeb1831fa
      - name: Generate keystore
        script: |
          keytool -genkey -v \
            -keystore keystore.jks \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -alias upload \
            -storepass $CM_KEYSTORE_PASSWORD \
            -keypass $CM_KEY_PASSWORD \
            -dname "CN=OxygeTV, OU=Mobile, O=OxygeTV, L=Unknown, ST=Unknown, C=US" \
            -noprompt
<<<<<<< HEAD
      
      - name: Update Gradle Config
        script: |
          echo "android.signingConfigs.release.storeFile=../keystore.jks" >> android/app/build.gradle
          echo "android.signingConfigs.release.storePassword=$CM_KEYSTORE_PASSWORD" >> android/app/build.gradle
          echo "android.signingConfigs.release.keyAlias=upload" >> android/app/build.gradle
          echo "android.signingConfigs.release.keyPassword=$CM_KEY_PASSWORD" >> android/app/build.gradle
      
      - name: Build Android
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease
    
=======
      - name: Set up local properties
        script: echo "sdk.dir=$ANDROID_SDK_ROOT" > "./android/local.properties"
      - name: Build Android
        script: |
          cd android
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=../keystore.jks \
            -Pandroid.injected.signing.store.password=$CM_KEYSTORE_PASSWORD \
            -Pandroid.injected.signing.key.alias=upload \
            -Pandroid.injected.signing.key.password=$CM_KEY_PASSWORD
>>>>>>> 29ee1b224115e2cbd0fda8c7d2d6682aeb1831fa
    artifacts:
      - android/app/build/outputs/**/*.apk
    publishing:
      email:
        recipients:
          - your.email@example.com # Замените на вашу почту